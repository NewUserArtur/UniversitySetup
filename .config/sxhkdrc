alt+ shift + c
    python3 MYPATH/.scripts/course_selection.py

alt + shift + y
    python3 MYPATH/.scripts/year_selection.py

alt + shift + v
    python3 MYPATH/.scripts/alter_master.py

alt + shift + l
    python3 MYPATH/.scripts/select_lecture.py

alt + shift + m
    zathura MYPATH/current-course/master.pdf

alt + shift + r
    xdg-open MYPATH/current-course

alt + shift + j
    x-terminal-emulator --workdir=MYPATH/current-course

alt + shift + t
    rofi-theme-selector

alt + shift + i
    python3 MYPATH/.scripts/status_bar_toggle.py
